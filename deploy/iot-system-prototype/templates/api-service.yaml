apiVersion: v1
kind: Service
metadata:
  name: api
  labels:
    service: api
  namespace: iot-system-pt
spec:
  selector:
    service: api
  ports:
  - port: 8080
    name: "8080"
    targetPort: 8080
