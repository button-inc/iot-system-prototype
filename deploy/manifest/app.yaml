apiVersion: apps/v1
kind: Deployment
metadata:
  name: custom-app
  labels:
    app: iotapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: custom-app
  template:
    metadata:
      labels:
        app: custom-app
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: custom-app
          image: ghcr.io/button-inc/iot-system-prototype/app:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: "16Mi"
              cpu: "40m"
            limits:
              memory: "128Mi"
              cpu: "100m"
---
apiVersion: v1
kind: Service
metadata:
  name: custom-app-service
spec:
  selector:
    app: custom-app
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
